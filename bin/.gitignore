!RetrieveSymbols.exe
dbghelp.dll
symsrv.dll

# Exclude files generated by Incremental LTCG in VS2015
# Their location does not seem to be affected by the Intermediate Directory
# setting, even though it seems like it should.
*.iobj
*.ipdb

# Exclude flamegraph.pl so that users can install it
flamegraph.pl
